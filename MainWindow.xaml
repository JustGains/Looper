
                <Setter Property="Width" Value="14"/>
                <Setter Property="Height" Value="14"/>
                <Setter Property="Stretch" Value="Uniform"/>
                <Setter Property="Margin" Value="0,0,6,0"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Fill" Value="Transparent"/>
                <Setter Property="Data">
                    <Setter.Value>
                        <RectangleGeometry Rect="0,0,0,0"/>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Tool}" Value="{x:Static models:CliTool.ClaudeCode}">
                        <Setter Property="Data" Value="{StaticResource ClaudeIconGeometry}"/>
                        <Setter Property="Fill" Value="{StaticResource ClaudeBrandBrush}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Tool}" Value="{x:Static models:CliTool.Codex}">
                        <Setter Property="Data" Value="{StaticResource OpenAiIconGeometry}"/>
                        <Setter Property="Fill" Value="White"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
            <DataTemplate x:Key="ToolOptionTemplate">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Path Style="{StaticResource ToolIconStyle}"/>
                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
                </StackPanel>
            </DataTemplate>

            <!-- Palette -->
            <SolidColorBrush x:Key="BgWindow" Color="#1e1e1e"/>
            <SolidColorBrush x:Key="BgSurface" Color="#252526"/>
            <SolidColorBrush x:Key="BgToolbar" Color="#2d2d30"/>
            <SolidColorBrush x:Key="BgInput" Color="#1b1b1c"/>
            <SolidColorBrush x:Key="BgConsole" Color="#0f0f10"/>
            <SolidColorBrush x:Key="FgPrimary" Color="#eaeaea"/>
            <SolidColorBrush x:Key="FgMuted" Color="#a8a8ac"/>
            <SolidColorBrush x:Key="FgDim" Color="#76767c"/>
            <SolidColorBrush x:Key="FgAccent" Color="#7dc4ff"/>
            <SolidColorBrush x:Key="Border1" Color="#3f3f46"/>
            <SolidColorBrush x:Key="Border2" Color="#2a2a2d"/>
            <SolidColorBrush x:Key="Accent" Color="#0e639c"/>
            <SolidColorBrush x:Key="AccentHover" Color="#1177bb"/>
            <SolidColorBrush x:Key="Success" Color="#16825d"/>
            <SolidColorBrush x:Key="SuccessHover" Color="#1a9b70"/>
            <SolidColorBrush x:Key="Danger" Color="#b03030"/>
            <SolidColorBrush x:Key="DangerHover" Color="#cc3838"/>

            <!-- ========== Pill design system — restrained, Apple-adjacent ========== -->
            <!-- Shared surface: barely lifted from toolbar, hairline border -->
            <SolidColorBrush x:Key="PillSurface" Color="#1f1f23"/>
            <SolidColorBrush x:Key="PillHairline" Color="#2c2c32"/>
            <SolidColorBrush x:Key="PillLabelColor" Color="#7a7a82"/>

            <!-- Status accents: single saturated color per state, flat (no gradients) -->
            <SolidColorBrush x:Key="AccentEmerald" Color="#34d399"/>
            <SolidColorBrush x:Key="AccentEmeraldDim" Color="#2a3b35"/>
            <SolidColorBrush x:Key="AccentAmber" Color="#fbbf24"/>
            <SolidColorBrush x:Key="AccentAmberDim" Color="#3b3422"/>
            <SolidColorBrush x:Key="AccentRose" Color="#f87171"/>
            <SolidColorBrush x:Key="AccentRoseDim" Color="#3b2428"/>

            <!-- Progress track + fill for the TASKS pill -->
            <SolidColorBrush x:Key="PillTrack" Color="#2a2a30"/>

            <!-- Base pill: shared surface, hairline, generous radius, tight padding -->
            <Style x:Key="StatusPill" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource PillSurface}"/>
                <Setter Property="BorderBrush" Value="{StaticResource PillHairline}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Padding" Value="10,5,10,5"/>
                <Setter Property="Margin" Value="0,0,6,0"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
            <!-- Status variants share the surface; only the content color differs -->
            <Style x:Key="StatusPillOk" TargetType="Border" BasedOn="{StaticResource StatusPill}"/>
            <Style x:Key="StatusPillWarn" TargetType="Border" BasedOn="{StaticResource StatusPill}"/>
            <Style x:Key="StatusPillDanger" TargetType="Border" BasedOn="{StaticResource StatusPill}"/>

            <!-- Typography — legible label (no small-caps — too small to read at a glance) -->
            <Style x:Key="PillLabel" TargetType="TextBlock">
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="{StaticResource PillLabelColor}"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="TextOptions.TextFormattingMode" Value="Ideal"/>
            </Style>
            <Style x:Key="PillNumber" TargetType="TextBlock">
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Typography.NumeralAlignment" Value="Tabular"/>
                <Setter Property="TextOptions.TextFormattingMode" Value="Ideal"/>
            </Style>
            <Style x:Key="PillDot" TargetType="Ellipse">
                <Setter Property="Width" Value="6"/>
                <Setter Property="Height" Value="6"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Margin" Value="0,0,8,0"/>
            </Style>

            <!-- Thin 2px progress bar with fully-rounded caps (capsule style) -->
            <Style x:Key="MiniProgressBar" TargetType="ProgressBar">
                <Setter Property="Height" Value="3"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Minimum" Value="0"/>
                <Setter Property="Maximum" Value="100"/>
                <Setter Property="Foreground" Value="{StaticResource AccentEmerald}"/>
                <Setter Property="Background" Value="{StaticResource PillTrack}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ProgressBar">
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="1.5" ClipToBounds="True">
                                <Grid>
                                    <Rectangle Name="PART_Track"/>
                                    <Rectangle Name="PART_Indicator"
                                               HorizontalAlignment="Left"
                                               Fill="{TemplateBinding Foreground}"
                                               RadiusX="1.5" RadiusY="1.5"/>
                                </Grid>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Flat button -->
            <Style x:Key="FlatButton" TargetType="Button">
                <Setter Property="Background" Value="#3a3a3d"/>
                <Setter Property="Foreground" Value="{StaticResource FgPrimary}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Border1}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="10,5"/>
                <Setter Property="MinWidth" Value="64"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4" SnapsToDevicePixels="True">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                                  Margin="{TemplateBinding Padding}" RecognizesAccessKey="True"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#4a4a4e"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#2c2c2f"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="Bd" Property="Opacity" Value="0.45"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Primary Start/Stop -->
            <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource FlatButton}">
                <Setter Property="Background" Value="{StaticResource Success}"/>
                <Setter Property="BorderBrush" Value="#0f5f44"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Padding" Value="18,7"/>
                <Setter Property="MinWidth" Value="100"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="1" CornerRadius="4">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                                  Margin="{TemplateBinding Padding}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{StaticResource SuccessHover}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsRunning}" Value="True">
                        <Setter Property="Background" Value="{StaticResource Danger}"/>
                        <Setter Property="BorderBrush" Value="#7a1f1f"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>

            <!-- Inputs -->
            <Style TargetType="TextBox">
                <Setter Property="Background" Value="{StaticResource BgInput}"/>
                <Setter Property="Foreground" Value="{StaticResource FgPrimary}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Border1}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="6,4"/>
                <Setter Property="CaretBrush" Value="{StaticResource FgPrimary}"/>
                <Setter Property="SelectionBrush" Value="{StaticResource Accent}"/>
            </Style>

            <Style TargetType="Label">
                <Setter Property="Foreground" Value="{StaticResource FgDim}"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Padding" Value="0,0,6,0"/>
                <Setter Property="FontSize" Value="10"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
            </Style>

            <!-- ComboBox toggle button template used inside the ComboBox template -->
            <ControlTemplate x:Key="ComboToggleTemplate" TargetType="ToggleButton">
                <Border x:Name="Bd" Background="{StaticResource BgInput}"
                        BorderBrush="{StaticResource Border1}"
                        BorderThickness="1" CornerRadius="4"/>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource FgAccent}"/>
                    </Trigger>
                    <Trigger Property="IsChecked" Value="True">
                        <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource FgAccent}"/>
                        <Setter TargetName="Bd" Property="Background" Value="#232327"/>
                    </Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>

            <!-- ComboBox -->
            <Style TargetType="ComboBox">
                <Setter Property="Background" Value="{StaticResource BgInput}"/>
                <Setter Property="Foreground" Value="{StaticResource FgPrimary}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Border1}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="8,4"/>
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid SnapsToDevicePixels="True">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="22"/>
                                </Grid.ColumnDefinitions>
                                <ToggleButton x:Name="ToggleButton" Grid.ColumnSpan="2"
                                              Focusable="False" ClickMode="Press"
                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                              Template="{StaticResource ComboToggleTemplate}"
                                              Background="{TemplateBinding Background}"/>
                                <ContentPresenter x:Name="ContentSite" Grid.Column="0"
                                                  IsHitTestVisible="False"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                  Margin="{TemplateBinding Padding}"
                                                  VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                <Path Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center"
                                      Fill="{StaticResource FgMuted}" Data="M 0 0 L 8 0 L 4 5 Z"
                                      IsHitTestVisible="False"/>
                                <Popup x:Name="Popup" Placement="Bottom"
                                       IsOpen="{TemplateBinding IsDropDownOpen}"
                                       AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                    <Border Background="{StaticResource BgSurface}"
                                            BorderBrush="{StaticResource Border1}" BorderThickness="1"
                                            CornerRadius="4"
                                            MinWidth="{TemplateBinding ActualWidth}"
                                            MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                            Margin="0,2,0,0">
                                        <ScrollViewer>
                                            <StackPanel IsItemsHost="True"
                                                        KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Editable ComboBox (Model / Effort): free-text entry + dropdown -->
            <Style x:Key="EditableComboBox" TargetType="ComboBox">
                <Setter Property="IsEditable" Value="True"/>
                <Setter Property="Background" Value="{StaticResource BgInput}"/>
                <Setter Property="Foreground" Value="{StaticResource FgPrimary}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Border1}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="8,4"/>
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid SnapsToDevicePixels="True">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="22"/>
                                </Grid.ColumnDefinitions>
                                <Border x:Name="OuterBd" Grid.ColumnSpan="2"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="4"/>
                                <TextBox x:Name="PART_EditableTextBox" Grid.Column="0"
                                         Margin="{TemplateBinding Padding}"
                                         Background="Transparent" BorderThickness="0"
                                         Foreground="{StaticResource FgPrimary}"
                                         CaretBrush="{StaticResource FgPrimary}"
                                         SelectionBrush="{StaticResource Accent}"
                                         VerticalAlignment="Center"
                                         FocusVisualStyle="{x:Null}"/>
                                <ToggleButton Grid.Column="1" Focusable="False" ClickMode="Press"
                                              Background="Transparent"
                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border Background="Transparent">
                                                <Path HorizontalAlignment="Center" VerticalAlignment="Center"
                                                      Fill="{StaticResource FgMuted}" Data="M 0 0 L 8 0 L 4 5 Z"/>
                                            </Border>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <Popup x:Name="Popup" Placement="Bottom"
                                       IsOpen="{TemplateBinding IsDropDownOpen}"
                                       AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                    <Border Background="{StaticResource BgSurface}"
                                            BorderBrush="{StaticResource Border1}" BorderThickness="1"
                                            CornerRadius="4" Margin="0,2,0,0"
                                            MinWidth="{TemplateBinding ActualWidth}"
                                            MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                        <ScrollViewer>
                                            <StackPanel IsItemsHost="True"
                                                        KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                    <Setter TargetName="OuterBd" Property="BorderBrush" Value="{StaticResource FgAccent}"/>
                                </Trigger>
                                <Trigger Property="IsDropDownOpen" Value="True">
                                    <Setter TargetName="OuterBd" Property="BorderBrush" Value="{StaticResource FgAccent}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="ComboBoxItem">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{StaticResource FgPrimary}"/>
                <Setter Property="Padding" Value="10,5"/>
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBoxItem">
                            <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                    Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                                <ContentPresenter/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsHighlighted" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{StaticResource Accent}"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#1a4e74"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="CheckBox">
                <Setter Property="Foreground" Value="{StaticResource FgMuted}"/>
            </Style>

            <!-- Dark ContextMenu + MenuItem + Separator -->
            <Style TargetType="ContextMenu">
                <Setter Property="Background" Value="{StaticResource BgSurface}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Border1}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Foreground" Value="{StaticResource FgPrimary}"/>
                <Setter Property="Padding" Value="4"/>
                <Setter Property="HasDropShadow" Value="True"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ContextMenu">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4"
                                    Padding="{TemplateBinding Padding}"
                                    SnapsToDevicePixels="True">
                                <StackPanel IsItemsHost="True"
                                            KeyboardNavigation.DirectionalNavigation="Cycle"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style TargetType="MenuItem">
                <Setter Property="Foreground" Value="{StaticResource FgPrimary}"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Padding" Value="10,6"/>
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="MenuItem">
                            <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                    Padding="{TemplateBinding Padding}" CornerRadius="3"
                                    SnapsToDevicePixels="True">
                                <ContentPresenter ContentSource="Header" RecognizesAccessKey="True"
                                                  VerticalAlignment="Center"
                                                  TextElement.Foreground="{TemplateBinding Foreground}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsHighlighted" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{StaticResource Accent}"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.45"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style TargetType="Separator">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Separator">
                            <Border Background="{StaticResource Border2}" Height="1" Margin="4,4"/>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Dark ScrollBar -->
            <Style x:Key="ScrollThumb" TargetType="Thumb">
                <Setter Property="OverridesDefaultStyle" Value="True"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Thumb">
                            <Border x:Name="Bd" Background="#4a4a51" CornerRadius="3" Margin="2"/>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#6a6a72"/>
                                </Trigger>
                                <Trigger Property="IsDragging" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#7d7d86"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <ControlTemplate x:Key="InvisibleRepeatButton" TargetType="RepeatButton">
                <Border Background="Transparent"/>
            </ControlTemplate>
            <Style TargetType="ScrollBar">
                <Setter Property="Background" Value="#181819"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="OverridesDefaultStyle" Value="True"/>
                <Style.Triggers>
                    <Trigger Property="Orientation" Value="Vertical">
                        <Setter Property="Width" Value="12"/>
                        <Setter Property="MinWidth" Value="12"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ScrollBar">
                                    <Grid Background="{TemplateBinding Background}">
                                        <Track x:Name="PART_Track" IsDirectionReversed="True">
                                            <Track.DecreaseRepeatButton>
                                                <RepeatButton Command="ScrollBar.PageUpCommand"
                                                              Template="{StaticResource InvisibleRepeatButton}"/>
                                            </Track.DecreaseRepeatButton>
                                            <Track.IncreaseRepeatButton>
                                                <RepeatButton Command="ScrollBar.PageDownCommand"
                                                              Template="{StaticResource InvisibleRepeatButton}"/>
                                            </Track.IncreaseRepeatButton>
                                            <Track.Thumb>
                                                <Thumb Style="{StaticResource ScrollThumb}"/>
                                            </Track.Thumb>
                                        </Track>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Trigger>
                    <Trigger Property="Orientation" Value="Horizontal">
                        <Setter Property="Height" Value="12"/>
                        <Setter Property="MinHeight" Value="12"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ScrollBar">
                                    <Grid Background="{TemplateBinding Background}">
                                        <Track x:Name="PART_Track">
                                            <Track.DecreaseRepeatButton>
                                                <RepeatButton Command="ScrollBar.PageLeftCommand"
                                                              Template="{StaticResource InvisibleRepeatButton}"/>
                                            </Track.DecreaseRepeatButton>
                                            <Track.IncreaseRepeatButton>
                                                <RepeatButton Command="ScrollBar.PageRightCommand"
                                                              Template="{StaticResource InvisibleRepeatButton}"/>
                                            </Track.IncreaseRepeatButton>
                                            <Track.Thumb>
                                                <Thumb Style="{StaticResource ScrollThumb}"/>
                                            </Track.Thumb>
                                        </Track>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- Numeric stepper button (±) -->
            <Style x:Key="StepperButton" TargetType="RepeatButton">
                <Setter Property="Width" Value="22"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{StaticResource FgMuted}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Setter Property="Delay" Value="400"/>
                <Setter Property="Interval" Value="60"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RepeatButton">
                            <Border x:Name="Bd" Background="{TemplateBinding Background}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#3a3a3f"/>
                                    <Setter Property="Foreground" Value="{StaticResource FgPrimary}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{StaticResource Accent}"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Toolbar divider -->
            <Style x:Key="ToolbarDivider" TargetType="Border">
                <Setter Property="Width" Value="1"/>
                <Setter Property="Background" Value="{StaticResource Border1}"/>
                <Setter Property="Margin" Value="12,4"/>
            </Style>

            <!-- Pane header label -->
            <Style x:Key="PaneHeader" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{StaticResource FgMuted}"/>
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Padding" Value="4,0"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>

            <!-- Pane header bar -->
            <Style x:Key="PaneHeaderBar" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource BgToolbar}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Border2}"/>
                <Setter Property="BorderThickness" Value="1,1,1,0"/>
                <Setter Property="Padding" Value="8,5"/>
                <Setter Property="CornerRadius" Value="4,4,0,0"/>
            </Style>

            <!-- TabControl / TabItem dark -->
            <Style TargetType="TabControl">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="0"/>
            </Style>
            <Style TargetType="TabItem">
                <Setter Property="Foreground" Value="{StaticResource FgMuted}"/>
                <Setter Property="Padding" Value="10,4"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TabItem">
                            <Border x:Name="Bd" Background="Transparent" Padding="{TemplateBinding Padding}"
                                    BorderBrush="Transparent" BorderThickness="0,0,0,2">
                                <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#33333a"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource FgAccent}"/>
                                    <Setter Property="Foreground" Value="{StaticResource FgPrimary}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="PaneFrame" TargetType="Border">
                <Setter Property="BorderBrush" Value="{StaticResource Border2}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Background" Value="{StaticResource BgSurface}"/>
                <Setter Property="CornerRadius" Value="0,0,4,4"/>
            </Style>

            <Style TargetType="FlowDocumentScrollViewer">
                <Setter Property="Background" Value="{StaticResource BgSurface}"/>
                <Setter Property="Foreground" Value="{StaticResource FgPrimary}"/>
                <Setter Property="Padding" Value="14,10"/>
            </Style>

            <!-- ============ Markdig.Wpf dark theme ============ -->
            <Style x:Key="{x:Static md:Styles.DocumentStyleKey}" TargetType="FlowDocument">
                <Setter Property="FontFamily" Value="Segoe UI Variable, Segoe UI"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Foreground" Value="{StaticResource FgPrimary}"/>
                <Setter Property="Background" Value="{StaticResource BgSurface}"/>
                <Setter Property="PagePadding" Value="14,10"/>
                <Setter Property="LineHeight" Value="20"/>
            </Style>
            <Style x:Key="{x:Static md:Styles.ParagraphStyleKey}" TargetType="Paragraph">
                <Setter Property="Margin" Value="0,4,0,6"/>
            </Style>
            <Style x:Key="{x:Static md:Styles.Heading1StyleKey}" TargetType="Paragraph">
                <Setter Property="FontSize" Value="22"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="{StaticResource FgPrimary}"/>
                <Setter Property="Margin" Value="0,12,0,6"/>
            </Style>
            <Style x:Key="{x:Static md:Styles.Heading2StyleKey}" TargetType="Paragraph">
                <Setter Property="FontSize" Value="18"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="{StaticResource FgPrimary}"/>
                <Setter Property="Margin" Value="0,10,0,4"/>
            </Style>
            <Style x:Key="{x:Static md:Styles.Heading3StyleKey}" TargetType="Paragraph">
                <Setter Property="FontSize" Value="15"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="{StaticResource FgPrimary}"/>
                <Setter Property="Margin" Value="0,8,0,2"/>
            </Style>
            <Style x:Key="{x:Static md:Styles.Heading4StyleKey}" TargetType="Paragraph">
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="{StaticResource FgMuted}"/>
            </Style>
            <Style x:Key="{x:Static md:Styles.Heading5StyleKey}" TargetType="Paragraph"
                   BasedOn="{StaticResource {x:Static md:Styles.Heading4StyleKey}}"/>
            <Style x:Key="{x:Static md:Styles.Heading6StyleKey}" TargetType="Paragraph"
                   BasedOn="{StaticResource {x:Static md:Styles.Heading4StyleKey}}"/>
            <Style x:Key="{x:Static md:Styles.CodeStyleKey}" TargetType="Run">
                <Setter Property="FontFamily" Value="Cascadia Code, Cascadia Mono, Consolas"/>
                <Setter Property="FontSize" Value="12.5"/>
                <Setter Property="Background" Value="#2e2e33"/>
                <Setter Property="Foreground" Value="#f3c88a"/>
            </Style>
            <Style x:Key="{x:Static md:Styles.CodeBlockStyleKey}" TargetType="Paragraph">
                <Setter Property="FontFamily" Value="Cascadia Code, Cascadia Mono, Consolas"/>
                <Setter Property="FontSize" Value="12.5"/>
                <Setter Property="Background" Value="#17171a"/>
                <Setter Property="Foreground" Value="#e4e4e6"/>
                <Setter Property="Padding" Value="10,8"/>
                <Setter Property="Margin" Value="0,4,0,6"/>
            </Style>
            <Style x:Key="{x:Static md:Styles.QuoteBlockStyleKey}" TargetType="Section">
                <Setter Property="BorderBrush" Value="{StaticResource FgAccent}"/>
                <Setter Property="BorderThickness" Value="3,0,0,0"/>
                <Setter Property="Padding" Value="10,2"/>
                <Setter Property="Foreground" Value="{StaticResource FgMuted}"/>
            </Style>
            <Style x:Key="{x:Static md:Styles.HyperlinkStyleKey}" TargetType="Hyperlink">
                <Setter Property="Foreground" Value="{StaticResource FgAccent}"/>
                <Setter Property="TextDecorations" Value="Underline"/>
            </Style>
            <Style x:Key="{x:Static md:Styles.TableStyleKey}" TargetType="Table">
                <Setter Property="BorderBrush" Value="{StaticResource Border1}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CellSpacing" Value="0"/>
                <Setter Property="Margin" Value="0,4,0,6"/>
            </Style>
            <Style x:Key="{x:Static md:Styles.TableCellStyleKey}" TargetType="TableCell">
                <Setter Property="BorderBrush" Value="{StaticResource Border1}"/>
                <Setter Property="BorderThickness" Value="0,0,1,1"/>
                <Setter Property="Padding" Value="8,4"/>
            </Style>
            <Style x:Key="{x:Static md:Styles.TableHeaderStyleKey}" TargetType="TableCell">
                <Setter Property="BorderBrush" Value="{StaticResource Border1}"/>
                <Setter Property="BorderThickness" Value="0,0,1,1"/>
                <Setter Property="Padding" Value="8,4"/>
                <Setter Property="Background" Value="{StaticResource BgToolbar}"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
            </Style>
            <Style x:Key="{x:Static md:Styles.ThematicBreakStyleKey}" TargetType="Line">
                <Setter Property="Stroke" Value="{StaticResource Border1}"/>
                <Setter Property="StrokeThickness" Value="1"/>
            </Style>
            <Style x:Key="{x:Static md:Styles.TaskListStyleKey}" TargetType="CheckBox">
                <Setter Property="Foreground" Value="{StaticResource FgPrimary}"/>
                <Setter Property="Margin" Value="0,0,6,0"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="IsHitTestVisible" Value="False"/>
            </Style>
            <Style x:Key="{x:Static md:Styles.InsertedStyleKey}" TargetType="Span">
                <Setter Property="Foreground" Value="#7bd88f"/>
            </Style>
            <Style x:Key="{x:Static md:Styles.StrikeThroughStyleKey}" TargetType="Span">
                <Setter Property="TextDecorations" Value="Strikethrough"/>
                <Setter Property="Foreground" Value="{StaticResource FgDim}"/>
            </Style>
            <Style x:Key="{x:Static md:Styles.MarkedStyleKey}" TargetType="Span">
                <Setter Property="Background" Value="#4a3e00"/>
                <Setter Property="Foreground" Value="#ffe08a"/>
            </Style>

            <!-- Spinner reusable -->
            <Style x:Key="Spinner" TargetType="Grid">
                <Setter Property="Width" Value="16"/>
                <Setter Property="Height" Value="16"/>
                <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>   <!-- tab strip -->
            <RowDefinition Height="Auto"/>   <!-- toolbar -->
            <RowDefinition Height="*"/>      <!-- prompt + tasks -->
            <RowDefinition Height="6"/>      <!-- splitter -->
            <RowDefinition Height="260"/>    <!-- console -->
            <RowDefinition Height="Auto"/>   <!-- status bar -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220" MinWidth="160"/>
            <ColumnDefinition Width="4"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ================= TOOLBAR ================= -->
        <Border Grid.Row="1" Grid.ColumnSpan="3" Background="{StaticResource BgToolbar}"
                BorderBrush="{StaticResource Border1}" BorderThickness="1"
                CornerRadius="6" Padding="10,8" Margin="0,0,0,10"
                DataContext="{Binding SelectedProject.SelectedConversation}">
            <DockPanel LastChildFill="True">
                <!-- Settings icon pinned to the right -->
                <Button DockPanel.Dock="Right" Style="{StaticResource FlatButton}"
                        MinWidth="38" Padding="8,5" VerticalAlignment="Top"
                        Margin="6,0,0,0"
                        Click="OpenConfig_Click"
                        ToolTip="Open looper.conf in Notepad">
                    <TextBlock Text="⚙" FontSize="15" VerticalAlignment="Center"/>
                </Button>

                <WrapPanel Orientation="Horizontal" ItemHeight="32">
                <!-- tool -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                    <Label Content="TOOL"/>
                    <ComboBox Width="130"
                              ItemsSource="{Binding ToolOptions}"
                              SelectedItem="{Binding SelectedToolOption}"
                              ItemTemplate="{StaticResource ToolOptionTemplate}"/>
                </StackPanel>

                <!-- model -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                    <Label Content="MODEL"/>
                    <ComboBox Width="180" Style="{StaticResource EditableComboBox}"
                              ItemsSource="{Binding ModelSuggestions}"
                              Text="{Binding ModelText, UpdateSourceTrigger=LostFocus}"
                              ToolTip="Type any model id — passed as --model (Claude) / -m (Codex). Blank = CLI default."/>
                </StackPanel>

                <!-- effort -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                    <Label Content="EFFORT"/>
                    <ComboBox Width="110" Style="{StaticResource EditableComboBox}"
                              ItemsSource="{Binding EffortSuggestions}"
                              Text="{Binding EffortText, UpdateSourceTrigger=LostFocus}"
                              ToolTip="low / medium / high / xhigh / max. Blank = CLI default."/>
                </StackPanel>

                <Border Style="{StaticResource ToolbarDivider}"/>

                <!-- numerics (timeout moved to console header) -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                    <CheckBox Content="Ralph" Margin="0,0,10,0"
                              VerticalAlignment="Center"
                              IsChecked="{Binding RalphEnabled}"
                              ToolTip="When off, each Start does exactly one pass (no looping). Max Iter is ignored."/>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center"
                                Visibility="{Binding RalphEnabled, Converter={StaticResource BoolToVis}}">
                        <Label Content="MAX ITER"/>
                        <Border BorderBrush="{StaticResource Border1}" BorderThickness="1"
                                CornerRadius="4" Background="{StaticResource BgInput}"
                                Height="26">
                            <StackPanel Orientation="Horizontal">
                                <RepeatButton Style="{StaticResource StepperButton}"
                                              Click="MaxIterDown_Click" Content="−"/>
                                <TextBox x:Name="MaxIterBox" Width="36"
                                         Background="Transparent" BorderThickness="0"
                                         Text="{Binding MaxIterations, UpdateSourceTrigger=LostFocus}"
                                         VerticalContentAlignment="Center"
                                         HorizontalContentAlignment="Center"/>
                                <RepeatButton Style="{StaticResource StepperButton}"
                                              Click="MaxIterUp_Click" Content="+"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </StackPanel>

                <CheckBox Margin="4,0,8,0" VerticalAlignment="Center"
                          IsChecked="{Binding KeepContext}"
                          Content="Keep context"
                          ToolTip="Subsequent iterations resume the previous CLI session (Claude: --resume / Codex: exec resume). Both tools auto-compact when context grows large."/>

                </WrapPanel>
            </DockPanel>
        </Border>

        <!-- ================= PROJECT TAB STRIP ================= -->
        <Border Grid.Row="0" Grid.ColumnSpan="3" BorderBrush="{StaticResource Border1}"
                BorderThickness="0,0,0,1" Margin="0,0,0,6">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                <StackPanel Orientation="Horizontal">
                    <ListBox x:Name="ProjectTabs"
                             ItemsSource="{Binding Projects}"
                             SelectedItem="{Binding SelectedProject, Mode=TwoWay}"
                             Background="Transparent" BorderThickness="0" Padding="0"
                             HorizontalAlignment="Left"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.VerticalScrollBarVisibility="Disabled">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="0,0,1,-1"/>
                                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                <Setter Property="MinWidth" Value="120"/>
                                <Setter Property="Height" Value="30"/>
                                <Setter Property="SnapsToDevicePixels" Value="True"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Grid SnapsToDevicePixels="True">
                                                <Border x:Name="Bd"
                                                        Background="#232326"
                                                        BorderBrush="#1d1d20"
                                                        BorderThickness="0,0,1,0"
                                                        CornerRadius="6,6,0,0"/>
                                                <Border x:Name="Accent"
                                                        Background="Transparent"
                                                        BorderBrush="Transparent"
                                                        BorderThickness="0,2,0,0"
                                                        CornerRadius="6,6,0,0"/>
                                                <ContentPresenter x:Name="CP"
                                                                  VerticalAlignment="Center"
                                                                  Margin="14,0,8,0"
                                                                  TextElement.Foreground="{StaticResource FgMuted}"/>
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="#2a2a2e"/>
                                                    <Setter TargetName="CP" Property="TextElement.Foreground" Value="{StaticResource FgPrimary}"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="{StaticResource BgToolbar}"/>
                                                    <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Border1}"/>
                                                    <Setter TargetName="Bd" Property="BorderThickness" Value="1,1,1,0"/>
                                                    <Setter TargetName="Accent" Property="BorderBrush" Value="{StaticResource FgAccent}"/>
                                                    <Setter TargetName="CP" Property="TextElement.Foreground" Value="{StaticResource FgPrimary}"/>
                                                    <Setter Property="Panel.ZIndex" Value="1"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid VerticalAlignment="Center" ToolTip="{Binding HeaderTooltip}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Ellipse Grid.Column="0" Width="8" Height="8" Fill="#6a9955" Margin="0,0,7,0"
                                             VerticalAlignment="Center"
                                             Visibility="{Binding IsRunning, Converter={StaticResource BoolToVis}}"/>
                                    <TextBlock Grid.Column="1" Text="{Binding HeaderLabel}"
                                               VerticalAlignment="Center"
                                               TextTrimming="CharacterEllipsis"
                                               MaxWidth="180"/>
                                    <Button Grid.Column="2" Margin="10,0,-4,0" Width="18" Height="18"
                                            Padding="0"
                                            Background="Transparent" BorderThickness="0"
                                            Cursor="Hand" FontSize="13"
                                            Tag="{Binding}"
                                            Click="CloseTab_Click"
                                            ToolTip="Close project"
                                            IsEnabled="{Binding DataContext.CanCloseAny, RelativeSource={RelativeSource AncestorType=Window}}">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="XBd" Background="Transparent"
                                                        CornerRadius="3" Width="18" Height="18">
                                                    <TextBlock Text="✕" FontSize="10"
                                                               HorizontalAlignment="Center" VerticalAlignment="Center"
                                                               Foreground="{StaticResource FgDim}"
                                                               x:Name="XGlyph"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="XBd" Property="Background" Value="#5a2020"/>
                                                        <Setter TargetName="XGlyph" Property="Foreground" Value="#ffcccc"/>
                                                    </Trigger>
                                                    <Trigger Property="IsEnabled" Value="False">
                                                        <Setter Property="Opacity" Value="0.25"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <Button x:Name="AddProjectButton" Click="AddProject_Click"
                            ToolTip="Open project — recent or new workspace"
                            Background="Transparent" BorderThickness="0"
                            Cursor="Hand" Padding="10,0" Margin="4,0,0,0"
                            VerticalAlignment="Center" Height="30"
                            Foreground="{StaticResource FgMuted}"
                            FocusVisualStyle="{x:Null}">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="AddBd" Background="Transparent"
                                        CornerRadius="4" Padding="{TemplateBinding Padding}">
                                    <TextBlock Text="+" FontSize="18" FontWeight="SemiBold"
                                               VerticalAlignment="Center" HorizontalAlignment="Center"
                                               Foreground="{TemplateBinding Foreground}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="AddBd" Property="Background" Value="#2a2a2e"/>
                                        <Setter Property="Foreground" Value="{StaticResource FgPrimary}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- ================= CONVERSATION SIDEBAR ================= -->
        <Border Grid.Row="2" Grid.Column="0" Grid.RowSpan="3"
                Background="{StaticResource BgSurface}"
                BorderBrush="{StaticResource Border2}" BorderThickness="1"
                CornerRadius="4">
            <DockPanel>
                <Border DockPanel.Dock="Top" Background="{StaticResource BgToolbar}"
                        BorderBrush="{StaticResource Border2}" BorderThickness="0,0,0,1"
                        Padding="10,6">
                    <DockPanel LastChildFill="False">
                        <TextBlock Style="{StaticResource PaneHeader}" Text="CONVERSATIONS"/>
                        <Button DockPanel.Dock="Right" Click="AddConversation_Click"
                                ToolTip="Add a new conversation"
                                Background="Transparent" BorderThickness="0"
                                Cursor="Hand" Padding="6,0" Width="22" Height="22"
                                FocusVisualStyle="{x:Null}">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="Bd" Background="Transparent" CornerRadius="3">
                                        <TextBlock Text="+" FontSize="14" FontWeight="SemiBold"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                                   Foreground="{StaticResource FgMuted}" x:Name="G"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#3a3a3f"/>
                                            <Setter TargetName="G" Property="Foreground" Value="{StaticResource FgPrimary}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </DockPanel>
                </Border>
                <ListBox x:Name="ConversationList"
                         ItemsSource="{Binding SelectedProject.Conversations}"
                         SelectedItem="{Binding SelectedProject.SelectedConversation, Mode=TwoWay}"
                         Background="Transparent" BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="Bd" Background="Transparent"
                                                BorderBrush="Transparent" BorderThickness="3,0,0,0"
                                                Padding="8,6">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="#2b2b2e"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BgToolbar}"/>
                                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource FgAccent}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Ellipse Grid.Column="0" Width="7" Height="7" Fill="#6a9955" Margin="0,0,7,0"
                                         VerticalAlignment="Center"
                                         Visibility="{Binding IsRunning, Converter={StaticResource BoolToVis}}"/>
                                <Path Grid.Column="1" Style="{StaticResource ToolIconStyle}"
                                      VerticalAlignment="Center"/>
                                <Grid Grid.Column="2" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}"
                                               Foreground="{StaticResource FgPrimary}"
                                               VerticalAlignment="Center"
                                               TextTrimming="CharacterEllipsis"
                                               MouseLeftButtonDown="ConvName_MouseDown">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsEditingName}" Value="True">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBox Text="{Binding Name, UpdateSourceTrigger=LostFocus}"
                                             Tag="{Binding}"
                                             Padding="4,2" Margin="-2,-1"
                                             VerticalContentAlignment="Center"
                                             BorderThickness="1"
                                             Background="{StaticResource BgInput}"
                                             Foreground="{StaticResource FgPrimary}"
                                             CaretBrush="{StaticResource FgPrimary}"
                                             SelectionBrush="{StaticResource Accent}"
                                             BorderBrush="{StaticResource FgAccent}"
                                             IsVisibleChanged="ConvNameEdit_IsVisibleChanged"
                                             KeyDown="ConvNameEdit_KeyDown"
                                             LostFocus="ConvNameEdit_LostFocus">
                                        <TextBox.Style>
                                            <Style TargetType="TextBox">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsEditingName}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBox.Style>
                                    </TextBox>
                                </Grid>
                                <Button Grid.Column="3" Margin="6,0,0,0" Width="16" Height="16"
                                        Padding="0"
                                        Background="Transparent" BorderThickness="0"
                                        Cursor="Hand"
                                        Tag="{Binding}"
                                        Click="RemoveConversation_Click"
                                        ToolTip="Delete conversation">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="XBd" Background="Transparent"
                                                    CornerRadius="3" Width="16" Height="16">
                                                <TextBlock Text="✕" FontSize="10"
                                                           HorizontalAlignment="Center" VerticalAlignment="Center"
                                                           Foreground="{StaticResource FgDim}"
                                                           x:Name="XGlyph"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="XBd" Property="Background" Value="#5a2020"/>
                                                    <Setter TargetName="XGlyph" Property="Foreground" Value="#ffcccc"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <GridSplitter Grid.Row="2" Grid.Column="1" Grid.RowSpan="3"
                      HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                      Width="4" Background="{StaticResource Border2}" Margin="2,0"/>

        <!-- ================= PROMPT + TASKS ================= -->
        <Grid Grid.Row="2" Grid.Column="2" DataContext="{Binding SelectedProject.SelectedConversation}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Column="0">
                <Border DockPanel.Dock="Top" Style="{StaticResource PaneHeaderBar}">
                    <DockPanel LastChildFill="False">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Style="{StaticResource PaneHeader}" Text="PROMPT"/>
                            <TextBlock Foreground="{StaticResource FgDim}" FontSize="10"
                                       VerticalAlignment="Center" Margin="8,0,0,0"
                                       Text="{Binding PromptFile}"/>
                        </StackPanel>
                        <Border DockPanel.Dock="Right" Background="#4a3400"
                                BorderBrush="#6a4a00" BorderThickness="1"
                                CornerRadius="3" Padding="6,2"
                                VerticalAlignment="Center"
                                Visibility="{Binding PromptPendingChange, Converter={StaticResource BoolToVis}}">
                            <TextBlock Foreground="#ffd596" FontSize="10"
                                       Text="applies on next iteration"/>
                        </Border>
                    </DockPanel>
                </Border>
                <Border Style="{StaticResource PaneFrame}">
                    <Grid>
                            <TextBox x:Name="PromptBox"
                                     Text="{Binding Prompt, UpdateSourceTrigger=PropertyChanged}"
                                     AcceptsReturn="True" AcceptsTab="True"
                                     TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                                     BorderThickness="0" Background="Transparent"
                                     FontFamily="Cascadia Code, Cascadia Mono, Consolas" FontSize="13" Padding="10,8"
                                     TextChanged="PromptBox_TextChanged"
                                     SelectionChanged="PromptBox_SelectionChanged"
                                     PreviewKeyDown="PromptBox_PreviewKeyDown"
                                     LostFocus="PromptBox_LostFocus"
                                     MouseMove="PromptBox_MouseMove"
                                     MouseLeave="PromptBox_MouseLeave"/>

                            <!-- @-mention popup -->
                            <Popup x:Name="MentionPopup"
                                   PlacementTarget="{Binding ElementName=PromptBox}"
                                   Placement="Relative"
                                   AllowsTransparency="True" StaysOpen="False"
                                   Focusable="False" PopupAnimation="Fade">
                                <Border Background="{StaticResource BgSurface}"
                                        BorderBrush="{StaticResource Border1}" BorderThickness="1"
                                        CornerRadius="4" Padding="2" MinWidth="320">
                                    <ListBox x:Name="MentionList"
                                             Background="Transparent" BorderThickness="0"
                                             Foreground="{StaticResource FgPrimary}"
                                             MaxHeight="200"
                                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                        <ListBox.ItemContainerStyle>
                                            <Style TargetType="ListBoxItem">
                                                <Setter Property="Padding" Value="8,4"/>
                                                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="ListBoxItem">
                                                            <Border x:Name="Bd" Background="Transparent"
                                                                    Padding="{TemplateBinding Padding}"
                                                                    CornerRadius="3">
                                                                <ContentPresenter/>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter TargetName="Bd" Property="Background" Value="#2e2e33"/>
                                                                </Trigger>
                                                                <Trigger Property="IsSelected" Value="True">
                                                                    <Setter TargetName="Bd" Property="Background" Value="{StaticResource Accent}"/>
                                                                    <Setter Property="Foreground" Value="White"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </ListBox.ItemContainerStyle>
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding}"
                                                           FontFamily="Cascadia Code, Cascadia Mono, Consolas"
                                                           FontSize="12"
                                                           TextTrimming="CharacterEllipsis"/>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>
                                </Border>
                            </Popup>

                            <!-- Hover tooltip for @refs -->
                            <Popup x:Name="MentionTooltipPopup"
                                   PlacementTarget="{Binding ElementName=PromptBox}"
                                   Placement="Relative"
                                   AllowsTransparency="True" StaysOpen="True"
                                   Focusable="False" IsHitTestVisible="False">
                                <Border Background="{StaticResource BgSurface}"
                                        BorderBrush="{StaticResource Border1}" BorderThickness="1"
                                        CornerRadius="3" Padding="8,4">
                                    <TextBlock x:Name="MentionTooltipText"
                                               Foreground="{StaticResource FgPrimary}"
                                               FontFamily="Cascadia Code, Cascadia Mono, Consolas" FontSize="11"/>
                                </Border>
                            </Popup>
                        </Grid>
                </Border>
            </DockPanel>

            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Background="{StaticResource Border1}"/>

            <DockPanel Grid.Column="2">
                <Border DockPanel.Dock="Top" Style="{StaticResource PaneHeaderBar}">
                    <DockPanel LastChildFill="False">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Style="{StaticResource PaneHeader}" Text="TASKS"/>
                            <TextBlock Foreground="{StaticResource FgDim}" FontSize="10"
                                       VerticalAlignment="Center" Margin="8,0,0,0"
                                       Text="{Binding TasksFile}"/>
                        </StackPanel>
                        <CheckBox DockPanel.Dock="Right" VerticalAlignment="Center"
                                  IsChecked="{Binding DataContext.AutoScrollTasks, RelativeSource={RelativeSource AncestorType=Window}}"
                                  Content="Auto-scroll"/>
                    </DockPanel>
                </Border>
                <Border Style="{StaticResource PaneFrame}">
                    <TabControl x:Name="TasksTabs" Background="Transparent"
                                SelectedIndex="{Binding DataContext.TasksTabIndex, RelativeSource={RelativeSource AncestorType=Window}}">
                        <TabItem Header="Raw">
                            <TextBox x:Name="TasksBox"
                                     Text="{Binding TasksText, UpdateSourceTrigger=PropertyChanged}"
                                     AcceptsReturn="True" AcceptsTab="True"
                                     TextWrapping="NoWrap"
                                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                                     BorderThickness="0" Background="Transparent"
                                     FontFamily="Cascadia Code, Cascadia Mono, Consolas" FontSize="13" Padding="10,8"/>
                        </TabItem>
                        <TabItem Header="Markdown">
                            <md:MarkdownViewer x:Name="TasksMarkdown"
                                               Markdown="{Binding TasksText}"
                                               Background="{StaticResource BgSurface}"/>
                        </TabItem>
                    </TabControl>
                </Border>
            </DockPanel>
        </Grid>

        <GridSplitter Grid.Row="3" Grid.Column="2" Height="6"
                      HorizontalAlignment="Stretch" Background="{StaticResource Border1}"/>

        <!-- ================= CONSOLE ================= -->
        <DockPanel Grid.Row="4" Grid.Column="2"
                   DataContext="{Binding SelectedProject.SelectedConversation}">
            <Border DockPanel.Dock="Top" Style="{StaticResource PaneHeaderBar}">
                <DockPanel LastChildFill="True">
                    <!-- iteration / status / start-stop pinned to the right -->
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal"
                                VerticalAlignment="Top" Margin="8,0,0,0">
                        <TextBlock Text="{Binding IterationLabel}" Foreground="{StaticResource FgPrimary}"
                                   FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"
                                   Visibility="{Binding RalphEnabled, Converter={StaticResource BoolToVis}}"/>
                        <TextBlock Text="{Binding Status}" Foreground="{StaticResource FgAccent}"
                                   VerticalAlignment="Center" Margin="0,0,12,0"/>
                        <Button Style="{StaticResource PrimaryButton}"
                                Content="{Binding StartStopText}" Click="StartStop_Click"
                                Padding="14,4" MinWidth="80"/>
                    </StackPanel>

                    <WrapPanel Orientation="Horizontal" ItemHeight="28">
                    <!-- title + spinner -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                        <TextBlock Style="{StaticResource PaneHeader}" Text="STREAMING OUTPUT"/>
                        <Grid Style="{StaticResource Spinner}" Margin="8,0,0,0"
                              Visibility="{Binding IsRunning, Converter={StaticResource BoolToVis}}">
                            <Grid.RenderTransform>
                                <RotateTransform x:Name="SpinnerRotate" Angle="0"/>
                            </Grid.RenderTransform>
                            <Ellipse Width="14" Height="14" Stroke="#3a3a3f" StrokeThickness="2"/>
                            <Ellipse Width="14" Height="14" Stroke="{StaticResource FgAccent}" StrokeThickness="2"
                                     StrokeDashArray="4 20" StrokeDashCap="Round"/>
                            <Grid.Triggers>
                                <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="SpinnerRotate"
                                                             Storyboard.TargetProperty="Angle"
                                                             From="0" To="360" Duration="0:0:1.1"
                                                             RepeatBehavior="Forever"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Grid.Triggers>
                        </Grid>
                    </StackPanel>

                    <Border Width="1" Background="{StaticResource Border1}" Margin="0,3,8,3"/>

                    <!-- timeout + elapsed timers -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                        <Label Content="TIMEOUT"/>
                        <TextBox Width="52" Margin="0,0,10,0"
                                 Text="{Binding TimeoutSeconds, UpdateSourceTrigger=LostFocus}"
                                 VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center"
                                    Visibility="{Binding RalphEnabled, Converter={StaticResource BoolToVis}}">
                            <Label Content="ITER"/>
                            <TextBlock Text="{Binding IterationElapsedText}"
                                       Foreground="{StaticResource FgPrimary}"
                                       FontFamily="Cascadia Code, Cascadia Mono, Consolas" VerticalAlignment="Center"
                                       Margin="0,0,10,0"/>
                        </StackPanel>
                        <Label Content="TOTAL"/>
                        <TextBlock Text="{Binding TotalElapsedText}"
                                   Foreground="{StaticResource FgPrimary}"
                                   FontFamily="Cascadia Code, Cascadia Mono, Consolas" VerticalAlignment="Center"/>
                    </StackPanel>

                    <Border Width="1" Background="{StaticResource Border1}" Margin="0,3,8,3"/>

                    <!-- display toggles -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                        <CheckBox IsChecked="{Binding DataContext.WordWrapConsole, RelativeSource={RelativeSource AncestorType=Window}}"
                                  VerticalAlignment="Center" Margin="0,0,10,0" Content="Wrap"/>
                        <CheckBox IsChecked="{Binding DataContext.CollapseToolCalls, RelativeSource={RelativeSource AncestorType=Window}}"
                                  VerticalAlignment="Center" Margin="0,0,10,0" Content="Collapse tools"/>
                        <CheckBox x:Name="AutoScrollBox"
                                  IsChecked="{Binding DataContext.AutoScrollConsole, RelativeSource={RelativeSource AncestorType=Window}}"
                                  VerticalAlignment="Center" Margin="0,0,10,0" Content="Auto-scroll"/>
                        <Button Content="Clear Session" Style="{StaticResource FlatButton}"
                                Click="ClearConsole_Click" MinWidth="60"/>
                    </StackPanel>

                    </WrapPanel>
                </DockPanel>
            </Border>
            <!-- Chat/send bar — single unified input (no dual-box) -->
            <DockPanel DockPanel.Dock="Bottom" LastChildFill="True" Margin="0,8,0,0">
                <!-- Queue chips stacked above the input -->
                <ItemsControl DockPanel.Dock="Top"
                              ItemsSource="{Binding QueuedMessages}"
                              Visibility="{Binding HasQueuedMessages, Converter={StaticResource BoolToVis}}"
                              Margin="0,0,0,6">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#22242a"
                                    BorderBrush="{StaticResource Border2}" BorderThickness="1"
                                    CornerRadius="5" Padding="8,4" Margin="0,0,0,4">
                                <DockPanel LastChildFill="True">
                                    <Button DockPanel.Dock="Right" Content="✕"
                                            Tag="{Binding}" Click="RemoveQueued_Click"
                                            Background="Transparent" BorderThickness="0"
                                            Foreground="{StaticResource FgDim}" Cursor="Hand"
                                            Padding="6,0" MinWidth="0"
                                            ToolTip="Remove from queue"/>
                                    <TextBlock Text="{Binding Text}"
                                               TextTrimming="CharacterEllipsis"
                                               VerticalAlignment="Center"
                                               Foreground="{StaticResource FgPrimary}"
                                               FontFamily="Cascadia Code, Cascadia Mono, Consolas"
                                               FontSize="12"/>
                                </DockPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Single rounded input with inline Send -->
                <Border Background="{StaticResource BgInput}"
                        BorderBrush="{StaticResource Border1}" BorderThickness="1"
                        CornerRadius="8">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Style.Triggers>
                                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                    <Setter Property="BorderBrush" Value="{StaticResource FgAccent}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <DockPanel LastChildFill="True">
                        <Button DockPanel.Dock="Right" Content="Send"
                                Click="ChatSend_Click" Cursor="Hand"
                                MinWidth="64" Margin="4" Padding="14,6"
                                Foreground="White" FontWeight="SemiBold"
                                BorderThickness="0">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="Bd" Background="{StaticResource Accent}"
                                            CornerRadius="5" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background"
                                                    Value="{StaticResource AccentHover}"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="Bd" Property="Background"
                                                    Value="{StaticResource Accent}"/>
                                            <Setter TargetName="Bd" Property="Opacity" Value="0.85"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <TextBox x:Name="ChatInputBox"
                                 Text="{Binding ChatInput, UpdateSourceTrigger=PropertyChanged}"
                                 AcceptsReturn="True" TextWrapping="Wrap"
                                 MaxHeight="90" VerticalScrollBarVisibility="Auto"
                                 TextChanged="ChatInputBox_TextChanged"
                                 SelectionChanged="ChatInputBox_SelectionChanged"
                                 PreviewKeyDown="ChatInputBox_PreviewKeyDown"
                                 LostFocus="ChatInputBox_LostFocus"
                                 MouseMove="ChatInputBox_MouseMove"
                                 MouseLeave="ChatInputBox_MouseLeave"
                                 Background="Transparent"
                                 Foreground="{StaticResource FgPrimary}"
                                 CaretBrush="{StaticResource FgAccent}"
                                 BorderThickness="0"
                                 FontFamily="Cascadia Code, Cascadia Mono, Consolas" FontSize="13"
                                 VerticalContentAlignment="Center"
                                 Padding="12,8"/>
                    </DockPanel>
                </Border>
            </DockPanel>
            <!-- Pinned last response — sticks to the top of the output pane so
                 the user can keep reading the model's narrative while tool
                 calls scroll past below. Updates live as text streams in,
                 and swaps whenever a new text/thinking block starts. -->
            <Border DockPanel.Dock="Top"
                    Background="#1a1c1e"
                    BorderBrush="{StaticResource Border1}" BorderThickness="1"
                    CornerRadius="6" Padding="10,8"
                    Margin="0,0,0,6"
                    Visibility="{Binding HasPinnedText, Converter={StaticResource BoolToVis}}">
                <DockPanel LastChildFill="True">
                    <DockPanel DockPanel.Dock="Top" Margin="0,0,0,4">
                        <Ellipse DockPanel.Dock="Left" Width="6" Height="6"
                                 VerticalAlignment="Center" Margin="0,0,7,0">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="{StaticResource AccentEmerald}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding PinnedIsThinking}" Value="True">
                                            <Setter Property="Fill" Value="#c189e8"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock Text="{Binding PinnedLabel}"
                                   FontSize="10" FontWeight="SemiBold"
                                   Foreground="{StaticResource PillLabelColor}"
                                   VerticalAlignment="Center"/>
                    </DockPanel>
                    <ScrollViewer MaxHeight="110"
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">
                        <TextBlock Text="{Binding PinnedText}"
                                   TextWrapping="Wrap"
                                   FontFamily="Cascadia Code, Cascadia Mono, Consolas"
                                   FontSize="12"
                                   Foreground="{StaticResource FgPrimary}"
                                   LineHeight="17">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding PinnedIsThinking}" Value="True">
                                            <Setter Property="FontStyle" Value="Italic"/>
                                            <Setter Property="Foreground" Value="#b5b5bb"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </ScrollViewer>
                </DockPanel>
            </Border>
            <Border Style="{StaticResource PaneFrame}" Background="{StaticResource BgConsole}">
                <RichTextBox x:Name="ConsoleBox"
                             IsReadOnly="True" IsDocumentEnabled="True"
                             VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                             BorderThickness="0" Background="Transparent"
                             FontFamily="Cascadia Code, Cascadia Mono, Consolas" FontSize="12"
                             Foreground="#d4d4d4" Padding="10,8"
                             Block.LineHeight="16"/>
            </Border>
        </DockPanel>

        <!-- ================= STATUS BAR ================= -->
        <Border Grid.Row="5" Grid.ColumnSpan="3" Margin="0,6,0,0"
                Background="{StaticResource BgToolbar}"
                BorderBrush="{StaticResource Border1}" BorderThickness="1"
                CornerRadius="4" Padding="10,4"
                DataContext="{Binding SelectedProject.SelectedConversation}">
            <DockPanel LastChildFill="False">
                <TextBlock DockPanel.Dock="Left" Text="just code"
                           Foreground="#ffd200"
                           FontFamily="Poppins SemiBold, Segoe UI Variable, Segoe UI"
                           FontWeight="SemiBold" FontSize="16"
                           VerticalAlignment="Center" Margin="2,0,14,0"
                           TextOptions.TextRenderingMode="ClearType"
                           TextOptions.TextFormattingMode="Ideal"/>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center"
                            Visibility="{Binding HasSession, Converter={StaticResource BoolToVis}}">
                    <TextBlock Text="SESSION" Foreground="{StaticResource FgDim}"
                               FontSize="10" FontWeight="SemiBold"
                               VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBlock Text="{Binding CurrentSessionId}"
                               Foreground="{StaticResource FgAccent}"
                               FontFamily="Cascadia Code, Cascadia Mono, Consolas" FontSize="11"
                               VerticalAlignment="Center"
                               ToolTip="{Binding CurrentSessionId}"
                               TextTrimming="CharacterEllipsis" MaxWidth="360"/>
                </StackPanel>

                <!-- Loop-health pills -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="14,0,0,0">

                    <!-- TASKS — numbers + label; 2px capsule bar below -->
                    <Border Style="{StaticResource StatusPillOk}"
                            Visibility="{Binding TaskStatsVisible, Converter={StaticResource BoolToVis}}"
                            ToolTip="Completed and remaining checkboxes in tasks.md">
                        <Grid MinWidth="104">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <DockPanel Grid.Row="0" LastChildFill="False">
                                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Style="{StaticResource PillNumber}"
                                               Text="{Binding ClosedTasks, Mode=OneWay}"
                                               Foreground="{StaticResource AccentEmerald}"/>
                                    <TextBlock Style="{StaticResource PillNumber}"
                                               Text="/"
                                               Foreground="{StaticResource PillLabelColor}"
                                               FontWeight="Normal"
                                               Margin="2,0,2,0"
                                               Opacity="0.6"/>
                                    <TextBlock Style="{StaticResource PillNumber}"
                                               Text="{Binding TaskTotal, Mode=OneWay}"
                                               Foreground="{StaticResource FgPrimary}"
                                               FontWeight="Medium"
                                               Opacity="0.85"/>
                                </StackPanel>
                                <TextBlock DockPanel.Dock="Right"
                                           Text="TASKS"
                                           FontSize="11"
                                           FontWeight="SemiBold"
                                           Foreground="{StaticResource PillLabelColor}"
                                           VerticalAlignment="Center"
                                           Margin="10,0,0,0"
                                           TextOptions.TextFormattingMode="Ideal"/>
                            </DockPanel>
                            <ProgressBar Grid.Row="1"
                                         Style="{StaticResource MiniProgressBar}"
                                         Margin="0,5,0,0"
                                         Value="{Binding TaskProgressPercent, Mode=OneWay}"/>
                        </Grid>
                    </Border>

                    <!-- PROGRESS STALLED -->
                    <Border Style="{StaticResource StatusPillWarn}"
                            Visibility="{Binding IsCircuitHalfOpen, Converter={StaticResource BoolToVis}}"
                            ToolTip="Two iterations with no file changes — next iteration is nudged to land concrete edits.">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Style="{StaticResource PillDot}" Fill="{StaticResource AccentAmber}"/>
                            <TextBlock Style="{StaticResource PillLabel}"
                                       Text="PROGRESS STALLED"
                                       Foreground="{StaticResource AccentAmber}"/>
                        </StackPanel>
                    </Border>

                    <!-- CIRCUIT OPEN -->
                    <Border Style="{StaticResource StatusPillDanger}"
                            Visibility="{Binding IsCircuitOpen, Converter={StaticResource BoolToVis}}"
                            ToolTip="Three iterations with no file changes — loop paused. Edit the prompt or fix blockers and press Start to resume.">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Style="{StaticResource PillDot}" Fill="{StaticResource AccentRose}"/>
                            <TextBlock Style="{StaticResource PillLabel}"
                                       Text="CIRCUIT OPEN"
                                       Foreground="{StaticResource AccentRose}"/>
                        </StackPanel>
                    </Border>

                    <!-- EXIT SIGNAL -->
                    <Border Style="{StaticResource StatusPillOk}"
                            Visibility="{Binding ExitStatusVisible, Converter={StaticResource BoolToVis}}"
                            ToolTip="Model emitted EXIT_SIGNAL=true — loop ended early.">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Style="{StaticResource PillDot}" Fill="{StaticResource AccentEmerald}"/>
                            <TextBlock Style="{StaticResource PillLabel}"
                                       Text="{Binding ExitStatus}"
                                       Foreground="{StaticResource AccentEmerald}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="TIME" Foreground="{StaticResource FgDim}"
                               FontSize="10" FontWeight="SemiBold"
                               VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBlock Text="{Binding TotalElapsedText, FallbackValue='--:--', TargetNullValue='--:--'}"
                               Foreground="{StaticResource FgPrimary}"
                               FontFamily="Cascadia Code, Cascadia Mono, Consolas" FontSize="11"
                               VerticalAlignment="Center"/>
                    <TextBlock Text="·" Foreground="{StaticResource FgDim}"
                               FontSize="11" VerticalAlignment="Center" Margin="8,0"/>
                    <TextBlock Text="TOOLS" Foreground="{StaticResource FgDim}"
                               FontSize="10" FontWeight="SemiBold"
                               VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBlock Text="{Binding ToolCallCount, FallbackValue='0', TargetNullValue='0'}"
                               Foreground="{StaticResource FgPrimary}"
                               FontFamily="Cascadia Code, Cascadia Mono, Consolas" FontSize="11"
                               VerticalAlignment="Center"/>
                    <TextBlock Text="·" Foreground="{StaticResource FgDim}"
                               FontSize="11" VerticalAlignment="Center" Margin="8,0"/>
                    <TextBlock Text="TOKENS" Foreground="{StaticResource FgDim}"
                               FontSize="10" FontWeight="SemiBold"
                               VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBlock Text="{Binding TokenSummary, FallbackValue='—', TargetNullValue='—'}"
                               Foreground="{StaticResource FgPrimary}"
                               FontFamily="Cascadia Code, Cascadia Mono, Consolas" FontSize="11"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</Window>
